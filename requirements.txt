# study-notes-summarizer — Python dependencies
# On first run the script auto-installs any missing packages (zero-manual).
# To pre-install manually:  pip install -r requirements.txt
python-docx
pdfplumber
python-pptx
img2pdf
pytesseract
pillow
pymupdf
pdf2image
rarfile
openpyxl
faster-whisper
# pywin32: Windows-only (used by .doc via Word COM). The script auto-installs it
#          on Windows at first run, so it is intentionally NOT listed here.
#          Do not install on macOS / Linux (no matching package, and not needed).
